version: "3.8"

services:
  frontend:
    image: vocascan/frontend:latest
    restart: always
    tty: true
    environment:
      VOCASCAN_BASE_URL: "https://web.example.com/"
    ports:
      - "80:80"
